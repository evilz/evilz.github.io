---
import Layout from '~/layouts/PageLayout.astro';
import Headline from '~/components/blog/Headline.astro';
import { fetchPosts } from '~/utils/blog';
import { categoryDetails } from '~/data/categoryDetails';
import { getPermalink } from '~/utils/permalinks';

const posts = await fetchPosts();
const counts = new Map<string, number>();
for (const post of posts) {
  if (post.category?.slug) counts.set(post.category.slug, (counts.get(post.category.slug) || 0) + 1);
}
const metadata = {
  title: 'Catégories',
  description: 'Catégories historiques du blog.',
};
---

<Layout metadata={metadata}>
  <section class="px-4 sm:px-6 py-10 sm:py-14 lg:py-16 mx-auto max-w-6xl">
    <div class="rounded-2xl border border-gray-200 bg-white p-5 md:p-8 shadow-sm">
      <Headline
        subtitle="Les catégories historiques du blog sont conservées, avec leur description d’origine lorsque disponible."
        >Catégories</Headline
      >
      <div class="grid sm:grid-cols-2 gap-5">
        {
          categoryDetails.map((category) => (
            <a
              href={getPermalink(category.slug, 'category')}
              class="rounded-xl border border-gray-200 bg-white p-5 shadow-sm transition hover:border-[#d4951a]/70 hover:-translate-y-0.5 hover:shadow-md"
            >
              <div class="flex items-center justify-between gap-4">
                <h2 class="text-xl font-bold font-heading text-gray-900">{category.title}</h2>
                <span class="text-sm text-gray-500">{counts.get(category.slug) || 0} article(s)</span>
              </div>
              <p class="mt-3 text-gray-600">
                {category.description || 'Catégorie conservée depuis le blog précédent.'}
              </p>
            </a>
          ))
        }
      </div>
    </div>
  </section>
</Layout>
